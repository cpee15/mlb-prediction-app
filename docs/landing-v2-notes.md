# Landing V2 notes

Issue 678.

Adds `/landing-v2` as a review route.

Reuses My Dashboard board categories and the `/my-dashboard/solver/batch` output shape.

Displayed fields: entity_name, primary_reason, score, confidence, entity_type, team, opponent, metrics, reasoning.

CTA routes reuse existing app routes: `/my-dashboard`, `/`, and `/ai-data-assistant`.

No new scoring formulas or fake production plays were added.
